export enum UserRole {
  User = 'user',
  Admin = 'admin',
}

export enum SortNews {
  ID = '_id',
  CREATED = 'createdAt',
  UPDATED = 'updatedAt',
  TITLE = 'title',
  REPORTER = 'reporter',
}

export enum SortProjects {
  ID = '_id',
  TITLE = 'title',
}
