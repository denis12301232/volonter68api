{
  "name": "volunteer68-api",
  "version": "1.0.50",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "bun run --watch src/index.ts",
    "build": "bun build src/index.ts --target=bun --outfile=build/server.js",
    "start": "NODE_ENV=production bun build/server.js",
    "format": "prettier --write src/"
  },
  "dependencies": {
    "@bogeychan/elysia-logger": "^0.0.21",
    "@elysiajs/cors": "^1.0.2",
    "@elysiajs/jwt": "^1.0.2",
    "elysia": "latest",
    "mongoose": "^8.3.1"
  },
  "devDependencies": {
    "bun-types": "latest",
    "prettier": "^3.2.5"
  },
  "module": "src/index.js"
}
